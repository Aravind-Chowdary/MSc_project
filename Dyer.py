import math
import pycrypto

M=math.infi
N=(math.infi)+1
N>M
m={0,M}
c={0,N}
a={input('Please enter the word' )}
txt=m.issubset(a)
def key_gen(s, k):
    λ=0
    λ in s
    λ > 8 /3*M
    set= {2^λ,2^λ+1}
    k in set
    k > M^8/3

def enc(k, r):


