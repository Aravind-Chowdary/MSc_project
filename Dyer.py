import math

M=math.infi
N=(math.infi)+1
N>M
m={0,M}
c={0,N}
a={input('Please enter the word' )}
txt=m.issubset(a)
def key_gen(s, k):
    λ=0
    λ in s
    λ > 8 / 3l
    set= {2^λ,2^λ+1}
    k in set



